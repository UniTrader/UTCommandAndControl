<?xml version="1.0" encoding="UTF-8"?>
<aiscript xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" name="ut.cac.com.manager.findjob.assign.escort" xsi:noNamespaceSchemaLocation="http://utnas/~unitrader/XRebirthxsds/aiscripts.xsd" version="1">
  <params>
    <param name="params" default="false" comment="pass a single Table filled with the wanted param Values here to make calls via list possible (always has priority)"/>
    <param name="escortee" default="false" comment="Ship needing Escorts"/>
    <param name="destination" default="false" comment="Destination Space to where the Ship will be escorted to (will check on Zone Context changes)"/>
    <param name="destination_timeout" default="5min" comment="How long the ship will continue the Escort after arriving in Destination context (will check on Zone Context changes)"/>
    <param name="timeout" default="player.age + 2h" comment="Game Time until when the Ship will be Escorted (will check on Zone Context changes)"/>
    <param name="return_waypoint" default="false" comment="optional: Waypoint to move to first after Escort ended (to account for Range Waypoints)"/>
  </params>
  <init>

  </init>
  <interrupts>
  </interrupts>
  <attention min="unknown">
    <actions>
      <do_if value="$params">
        <do_if value="$params.$entity?">
          <set_value name="$entity" exact="$params.$entity"/>
        </do_if>
        <do_if value="$params.$range?">
          <set_value name="$range" exact="$params.$range"/>
        </do_if>
        <do_if value="$params.$ware?">
          <set_value name="$ware" exact="$params.$ware"/>
        </do_if>
        <do_if value="$params.$mininglist?">
          <set_value name="$mininglist" exact="$params.$mininglist"/>
        </do_if>
        <remove_value name="$params"/>
      </do_if>
      
      <!---
      ######################################
      Script is just a Dummy to avoid pointless debug spam !!!
      will be written later....
      #######################################
      -->
      
      
      </actions>
  </attention>
</aiscript>
